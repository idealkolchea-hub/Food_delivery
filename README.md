# BiteBlast — Spec-to-Scaffold App

> "From spec to scaffold in 60 seconds."

**BiteBlast** is a trust-first food delivery platform for India (₹640B market). This project is the scaffold — generated from the [BiteBlast Obsidian vault](https://github.com/your-org/biteblast-vault) using **Scaffold**: a spec-to-scaffold execution engine that reads structured vault data and produces a working Next.js codebase.

---

## What Was Generated

This scaffold was auto-generated by **Scaffold** from the BiteBlast vault:

| Source File | Output |
|-------------|--------|
| `source_flowConfig.js` | 81 Next.js pages + RBAC layout shell |
| `source_screenMap.json` | Route paths + screen metadata |
| `ethereal-canvas.md` | CSS design tokens (`tokens.css`) |
| `3_BiteBlast_FRD.md` | 8 API route stubs |
| `deep-research-report(1).md` | TypeScript interfaces (`types/index.ts`) |
| `4_BiteBlast_User_Stories.md` | Playwright test stubs (TODO) |

---

## Project Structure

```
biteblast-app/
├── src/
│   ├── app/
│   │   ├── customer/          # 27 Customer screens
│   │   ├── partner/           # 21 Restaurant screens
│   │   ├── agent/             # 11 Agent screens
│   │   ├── admin/             # 22 Admin screens
│   │   └── api/               # 8 API route stubs
│   ├── components/
│   │   ├── layout/            # RoleLayout, PageWrapper
│   │   └── ethereal-canvas/   # Glassmorphic design system (TODO)
│   ├── lib/
│   │   └── vault.ts           # Vault reader module
│   ├── styles/
│   │   └── tokens.css         # Ethereal Canvas design tokens
│   └── types/
│       └── index.ts           # TypeScript domain models
├── tests/                     # Playwright test stubs (TODO)
├── package.json
├── next.config.js
├── tsconfig.json
├── playwright.config.ts
└── .env.example
```

---

## Setup

```bash
# 1. Install dependencies
npm install

# 2. Copy and configure environment
cp .env.example .env.local

# 3. Set vault path (default: /home/netwin/Desktop/demo 2)
# VAULT_PATH=/path/to/vault

# 4. Run development server
npm run dev

# 5. Open http://localhost:3000
```

---

## Role-Based Flows

Navigate to any role:

| Role | URL | Screens | Brand Color |
|------|-----|---------|-------------|
| Customer | `/customer` | 27 | Deep red `#8a0f0a` |
| Restaurant | `/partner` | 21 | Amber `#944b03` |
| Delivery Agent | `/agent` | 11 | Forest green `#1a6b47` |
| Admin | `/admin` | 22 | Deep navy `#2c2c54` |

---

## Generated Routes

### Customer (27 screens)
`/customer/auth-login`, `/customer/auth-otp`, `/customer/auth-profile`, `/customer/home-guest`, `/customer/home-logged`, `/customer/restaurant-detail`, `/customer/kitchen-story`, `/customer/spice-garden`, `/customer/offers`, `/customer/cart`, `/customer/payment-select`, `/customer/checkout`, `/customer/payment-failed`, `/customer/order-confirm`, `/customer/delivery-track`, `/customer/order-rejected`, `/customer/rate-experience`, `/customer/order-history`, `/customer/raise-ticket`, `/customer/ticket-resolved`, `/customer/report-issue`, `/customer/wallet`, `/customer/wallet-topup`, `/customer/profile`, `/customer/addresses`, `/customer/notifications`, `/customer/delivery-feedback`

### Partner (21 screens)
`/partner/partner-welcome`, `/partner/partner-dashboard`, `/partner/partner-app`, ...

### Agent (11 screens)
`/agent/new-order-offer`, `/agent/pickup-map`, `/agent/pickup-confirm`, ...

### Admin (22 screens)
`/admin/admin-dashboard`, `/admin/admin-desktop`, `/admin/live-heatmap`, ...

---

## API Routes

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET/POST | `/api/customers` | Customer CRUD |
| GET/POST | `/api/restaurants` | Restaurant CRUD |
| GET/POST | `/api/orders` | Order management |
| POST | `/api/delivery/assign` | Agent delivery assignment |
| POST | `/api/auth` | Phone OTP authentication |
| GET/POST | `/api/wallet` | Wallet balance & transactions |
| GET/POST | `/api/tickets` | Support ticket management |
| POST | `/api/admin/refund` | Admin refund workflow |

---

## Design System — Ethereal Canvas

All screens use the **Ethereal Canvas** glassmorphic design system. See `src/styles/tokens.css` for CSS variables:

- Role brand colors with gradients
- Glassmorphic cards and buttons
- Dark theme base (`#0f0f1a`)
- SF Pro / Apple system typography
- 12px blur backdrop filters

---

## Current State

- [x] Role hubs added for Customer, Partner, Agent, and Admin entry points
- [x] Shared route renderer replaces scaffold placeholders across the generated screen set
- [x] Customer demo flow works end-to-end: browse → cart → checkout → payment → confirmation → tracking
- [x] Mock API handlers now return structured demo data for auth, customers, restaurants, orders, wallet, tickets, delivery assignment, and refunds
- [x] Shared navigation now links to real routes instead of dead tab paths

## Next Steps

- [ ] Replace the mock data layer with a persistent database
- [ ] Add real OTP / payment / notification integrations
- [ ] Add Playwright coverage for the main happy paths and API contracts
- [ ] Add live map and GPS event streaming for delivery tracking

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Framework | Next.js 14 (App Router) |
| Language | TypeScript |
| Styling | CSS Modules + CSS Variables |
| Testing | Playwright |
| Auth | NextAuth.js + Phone OTP (Phase 2) |
| Deployment | Vercel (planned) |

---

## Vault Context

This scaffold was built from the BiteBlast Obsidian vault. The vault contains:

- **PRD** (9 goals, 78 screens, 4 stakeholders)
- **BRD** (revenue model, 20 business rules, KPIs)
- **FRD** (7 modules, 15+ API endpoints, use cases)
- **User Stories** (32 stories across 4 flows)
- **Trust Kernel** (6-component anti-fraud architecture blocking 8 fraud categories)
- **82-screen design** from Google Stitch (Ethereal Canvas glassmorphic)

---

*Generated by Scaffold v0.1 — powered by Claude Code + Codex multi-agent debate*
# BiteBlast
